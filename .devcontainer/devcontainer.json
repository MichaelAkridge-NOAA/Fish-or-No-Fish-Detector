{
  "name": "Yes Fish / No Fish",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  // run once per Codespace: cd into game dir, install deps, quick smoke-run
  "postCreateCommand": "cd .devcontainer/yes-no-fish && pip install -r requirements.txt && python app.py",

  // run on every restart: cd and run the game
  "postStartCommand":  "cd .devcontainer/yes-no-fish && python app.py"
}
